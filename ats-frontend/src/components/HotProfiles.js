import {
  Table,
  TableBody,
  TableCell,
  TableContainer,
  TableRow,
  Paper,
  Button,
  Typography,
  TablePagination,
} from "@mui/material";
import { Link } from "react-router-dom";

const HotProfiles = ({ candidates }) => {
  return <div>this is hot profiles tab</div>;
};

export default HotProfiles;
